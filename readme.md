# BaseCSS
BaseCSS is a default colleciton of css documents that I cultivate and use for my web projects.  
This collection operates with an init.css file that allows you to import all documents into one link using
```html
<link rel="stylesheet" type="text/css" href="<path_to_file>/global/init.css">
```
